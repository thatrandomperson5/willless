{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Nimble Build",
            "command": "nimble",
            "args": ["build", "--verbose"],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Nimble test",
            "command": "nimble",
            "args": ["test", "--verbose"],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "type": "shell",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}